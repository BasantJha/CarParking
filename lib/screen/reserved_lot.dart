import 'package:flutter/material.dart';
class ReservedLotDetails extends StatelessWidget {
  const ReservedLotDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(      backgroundColor: Colors.yellow.shade200,body: Center(child: Container(child: Text("Reserved Coming Soon"),)),);
  }
}

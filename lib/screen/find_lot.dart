import 'package:flutter/cupertino.dart';

class FindLot extends StatelessWidget {
  const FindLot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("FindLot"),));
  }
}
